{
  "name": "ideate-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Thin root: the Next app lives in app/, the Agent Link service in ideate-relay/ (Go).",
  "scripts": {
    "postinstall": "npm --prefix app install",
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "start": "npm --prefix app run start",
    "lint": "npm --prefix app run lint",
    "typecheck": "npm --prefix app run typecheck",
    "relay:dev": "cd ideate-relay && go run ./cmd/server",
    "relay:test": "cd ideate-relay && go vet ./... && go test ./...",
    "relay:build": "cd ideate-relay && go build -o server ./cmd/server",
    "relay:docker": "docker build --platform=linux/amd64 -t hasathcharu/ideate-relay ideate-relay",
    "relay:docker-push": "docker build --platform=linux/amd64 -t hasathcharu/ideate-relay ideate-relay && docker push hasathcharu/ideate-relay"
  }
}
